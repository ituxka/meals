import 'package:flutter/material.dart';

class FavoritesScreen extends StatelessWidget {
  static String title = 'Favorites';

  @override
  Widget build(BuildContext context) {
    return Text('FavoritesScreen StatelessWidget');
  }
}
